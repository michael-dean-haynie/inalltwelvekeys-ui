// pu@ come back to this once I've refactored the exercise code to be run in real time and after the fact
// it needs to be able to run deterministically with starting inputs and then either real time inputs or recorded inputs simulated faster
export interface ExerciseExecution {
  id?: number;
  exerciseHash: string;
}
